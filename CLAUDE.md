# agent-push

This repo sets up iPhone push notifications for AI coding agents.

If the user asks you to set it up, **read `AGENTS.md` and follow it precisely** — it is the
step-by-step runbook (collect the user's Bark URL + optional encryption key, install
`notify.sh` + config, wire Codex/Claude Code, test). Merge into existing config; never clobber.
